import { orderArr, shoppingCarArr } from './../../type/index';

const goodsClassificationList = {}; // 商品分类列表

const goodsClassificationTree = {}; // 商品分类树

const goodsList = {}; // 商品列表

const shoppingCar: shoppingCarArr = []; // 购物车

const userInfo = {}; // 用户信息

const marketInfo = {}; // 商城信息

const userOrder: orderArr = [];

export {
  goodsClassificationList,
  goodsClassificationTree,
  goodsList,
  shoppingCar,
  userInfo,
  marketInfo,
  userOrder,
};

